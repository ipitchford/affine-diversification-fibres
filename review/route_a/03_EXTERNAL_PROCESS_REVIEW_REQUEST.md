# External process-theory review request — OUTSTANDING

This file is a request, not a review receipt. It must not be counted as independent evidence.

Please examine the theorem titled `Fixed-stem count and ordered-node-age congruence` and the signal-band theorem that depends on it. In particular, verify:

1. the fixed stem-age and stem-survival conditioning;
2. the geometric tip-count parameter `p=1/F(T)`;
3. the conditional i.i.d. unordered node-age CDF and ordered density;
4. the disjoint-union sample space and counting-times-Lebesgue base measure;
5. every factorial/ordering normalizer;
6. the finite deterministic-survival-event convention and event sides;
7. the statement that topology has been marginalized and that no complete labelled/oriented/ranked topology law is claimed;
8. whether any major conditioning error invalidates the count/node-age factorization used by the Route A band.

An acceptable receipt must identify the reviewer, declare relevant expertise and conflicts, cite the exact candidate hash, record the materials examined, and state `no unresolved major conditioning error` or enumerate blocking corrections. Author-agent self-review does not satisfy this gate.
